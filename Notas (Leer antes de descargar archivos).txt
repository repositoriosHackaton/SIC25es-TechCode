Notas
Estan los 3 archivos .csv para el modelo

Estan tambien dos archivo .ipyn y 2 .py

Los .ipyn son para colab y si se quiere solo se copia el codigo en kaggle notebooks ya que ahi se dispone de dos GPU y para eso es el archivo de Entrenamiento_ChatBot_2GPU
ahi se puede ejecutar en paralelo las GPUs y aumentar los parametres ya que en una sola solo se ejecuta con float16 y en paralelo ocupa float32.

Ya sea si se ejecuta en forma local, colab o kaggle hay que cambiar las rutas de los archivos para que no de error.

De momento aun no se ha completado el entrenmiento, el modelo que se genera despues del entrenamiento se guarda automaticamente y se necesita que se descargue para poder hacer
pruebas en combinancion con el modelo para la camara.
